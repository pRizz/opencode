---
phase: 23-during-2fa-setup-make-the-server-automatically-set-the-google-authenticator-file-in-the-appropriate-user-s-home-folder-instead-of-asking-the-user-to-run-the-command-on-their-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode-broker/src/auth/otp.rs
  - packages/opencode-broker/src/ipc/protocol.rs
  - packages/opencode-broker/src/ipc/handler.rs
  - packages/opencode/src/auth/broker-client.ts
  - packages/opencode/src/server/routes/auth.ts
autonomous: true
must_haves:
  truths:
    - "During TOTP setup, the server writes `~/.google_authenticator` for the authenticated user without requiring a shell command."
    - "If the broker is unavailable or cannot write the file, the setup flow falls back to a manual command with a clear status indicator."
    - "TOTP verification errors no longer instruct users to run a setup command when auto-setup succeeded."
  artifacts:
    - path: "packages/opencode-broker/src/auth/otp.rs"
      provides: "Privileged write for google_authenticator file with ownership and permissions."
      exports: ["write_google_authenticator"]
    - path: "packages/opencode-broker/src/ipc/protocol.rs"
      provides: "New IPC method + params for OTP setup write."
      contains: "SetupOtp"
    - path: "packages/opencode-broker/src/ipc/handler.rs"
      provides: "Broker handler that resolves session user and writes file."
      contains: "handle_setup_otp"
    - path: "packages/opencode/src/auth/broker-client.ts"
      provides: "Client method to request OTP setup write."
      exports: ["setupOtp"]
    - path: "packages/opencode/src/server/routes/auth.ts"
      provides: "TOTP setup bootstrap data with auto-setup status and fallback."
      contains: "injectTotpSetupBootstrap"
  key_links:
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/opencode/src/auth/broker-client.ts"
      via: "BrokerClient.setupOtp()"
      pattern: "setupOtp\\("
    - from: "packages/opencode/src/auth/broker-client.ts"
      to: "packages/opencode-broker/src/ipc/protocol.rs"
      via: 'method: "setupotp"'
      pattern: "setupotp"
    - from: "packages/opencode-broker/src/ipc/handler.rs"
      to: "packages/opencode-broker/src/auth/otp.rs"
      via: "write_google_authenticator"
      pattern: "write_google_authenticator"
---

<objective>
Enable automatic creation of `~/.google_authenticator` during TOTP setup using the privileged broker, with a graceful manual fallback when broker access is unavailable.

Purpose: Remove the manual shell command step while keeping a safe fallback path if the broker cannot perform the write.
Output: Broker IPC support for OTP file creation, server route wiring, and updated bootstrap payload for the setup UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-two-factor-authentication/10-07-SUMMARY.md
@.planning/phases/22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app/22-02-SUMMARY.md
@packages/opencode/src/auth/totp-setup.ts
@packages/opencode/src/server/routes/auth.ts
@packages/opencode/src/auth/broker-client.ts
@packages/opencode-broker/src/auth/otp.rs
@packages/opencode-broker/src/ipc/protocol.rs
@packages/opencode-broker/src/ipc/handler.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add broker IPC support for OTP setup file creation</name>
  <files>packages/opencode-broker/src/auth/otp.rs, packages/opencode-broker/src/ipc/protocol.rs, packages/opencode-broker/src/ipc/handler.rs</files>
  <action>
Implement a privileged write path for `~/.google_authenticator` using the broker:
- Add a new IPC method (e.g., `setupotp`) with params including `session_id` and `secret` (avoid passing raw home/uid/gid from the server; resolve via `UserSessionStore`).
- In `otp.rs`, add a `write_google_authenticator(home, uid, gid, secret)` helper that:
  - Uses the same file content format as `getGoogleAuthenticatorSetupCommand()` (TOTP_AUTH, RATE_LIMIT 3 30, WINDOW_SIZE 3, DISALLOW_REUSE) with no backup codes.
  - Refuses to overwrite existing `.google_authenticator` and returns a typed error/result (e.g., already_configured).
  - Writes atomically (temp file + rename), sets mode 0400, and chowns to the target user.
  - Returns structured error codes for permission denied, invalid home, and write failure.
- In `handler.rs`, map `setupotp` to the write helper and return explicit `error_code` values in the response data.
- Update protocol response types to include `error_code` and `already_configured`/`written` flags for the setup flow.
- Add unit tests for file creation behavior using a temp dir; allow EPERM to skip chown-sensitive assertions without failing.
Avoid new dependencies; reuse existing nix/libc utilities already used for PTY ownership.
  </action>
  <verify>Run `cargo test` in `packages/opencode-broker`.</verify>
  <done>The broker can create `.google_authenticator` with correct ownership and returns structured status codes without overwriting existing files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire server setup route to broker and expose auto-setup status</name>
  <files>packages/opencode/src/auth/broker-client.ts, packages/opencode/src/server/routes/auth.ts</files>
  <action>
Wire the TOTP setup route to the new broker capability with fallback behavior:
- Add `setupOtp(sessionId, secret)` to `BrokerClient` to call the new IPC method and map response data into a typed result (written/alreadyConfigured/errorCode).
- In canonical `GET /auth/totp/setup` (legacy `/auth/2fa/setup` alias retained), after generating the TOTP secret, call `setupOtp` and derive a `setupStatus` value for the UI:
  - `created` when file write succeeded
  - `already_configured` when broker reports existing file
  - `manual_required` when broker unavailable or write fails
- Only include `setupCommand` in the bootstrap payload when `manual_required` (use existing `getGoogleAuthenticatorSetupCommand`).
- Update canonical `/auth/totp/verify` error messages (legacy `/auth/2fa/verify` alias retained) to remove "run the setup command" references; instead mention "ensure setup completed" and, if `manual_required`, refer to the command shown on the setup page.
- Ensure broker-unavailable paths return the setup page with manual fallback instead of a hard failure.
  </action>
  <verify>Run `bun test packages/opencode/test/server/routes/auth.test.ts`.</verify>
  <done>TOTP setup page uses broker auto-provisioning with a manual fallback when broker access fails, and verification errors no longer require users to run a command when auto-setup succeeds.</done>
</task>

</tasks>

<verification>
- `cargo test` in `packages/opencode-broker`.
- `bun test packages/opencode/test/server/routes/auth.test.ts`.
</verification>

<success_criteria>

- Auto-setup writes `~/.google_authenticator` with correct ownership and 0400 permissions for the authenticated user.
- Setup page shows a status indicating auto-setup success or manual fallback when broker access is unavailable.
- Verification errors no longer instruct users to run a setup command unless manual fallback is in effect.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-during-2fa-setup-make-the-server-automatically-set-the-google-authenticator-file-in-the-appropriate-user-s-home-folder-instead-of-asking-the-user-to-run-the-command-on-their-machine/23-01-SUMMARY.md`
</output>

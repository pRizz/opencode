---
phase: 23-during-2fa-setup-make-the-server-automatically-set-the-google-authenticator-file-in-the-appropriate-user-s-home-folder-instead-of-asking-the-user-to-run-the-command-on-their-machine
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - packages/app/src/totp-setup/setup.tsx
autonomous: true
must_haves:
  truths:
    - "The TOTP setup UI shows an auto-setup status instead of a command step by default."
    - "When the broker cannot create the file, the UI clearly shows a manual command fallback and keeps verification available."
    - "If TOTP is already configured, the UI indicates this without attempting to overwrite the file."
  artifacts:
    - path: "packages/app/src/totp-setup/setup.tsx"
      provides: "TOTP setup UI with auto-setup status and fallback rendering."
      contains: "TotpSetupApp"
  key_links:
    - from: "packages/app/src/totp-setup/setup.tsx"
      to: "window.__OPENCODE_TOTP_SETUP__"
      via: "setupStatus-based rendering"
      pattern: "setupStatus"
    - from: "packages/app/src/totp-setup/setup.tsx"
      to: "setupCommand"
      via: "manual fallback section"
      pattern: "setupCommand"
---

<objective>
Update the TOTP setup UI to remove the command step in the normal path, show automatic setup status, and surface a manual fallback only when required.

Purpose: Align the UI with server-side auto-provisioning while preserving a clear manual path if the broker is unavailable.
Output: Updated SolidJS setup page rendering and messaging for auto-setup and fallback states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app/22-01-SUMMARY.md
@packages/app/src/totp-setup/setup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setup status handling to bootstrap and layout</name>
  <files>packages/app/src/totp-setup/setup.tsx</files>
  <action>
Update the bootstrap typing and UI structure to reflect auto-setup status:
- Extend `TotpSetupBootstrap` with a `setupStatus` field (e.g., created | already_configured | manual_required) and optional `setupMessage`.
- Remove the default "run command" step; instead show a status banner that indicates whether the server created the file, the file already exists, or manual setup is required.
- Keep QR code + manual secret entry as the first step.
- Render the command copy block only when `setupStatus === "manual_required"` and a `setupCommand` is present.
  </action>
  <verify>Run `bun run typecheck` in `packages/app`.</verify>
  <done>The setup page shows an auto-setup status by default and only displays the manual command block on fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Align form messaging with auto-setup and fallback states</name>
  <files>packages/app/src/totp-setup/setup.tsx</files>
  <action>
Adjust user messaging to reflect the new setup flow:
- Update verification error text to avoid mentioning a command unless `setupStatus` is manual-required.
- If `already_configured`, display a warning note but keep the verify flow enabled so users can confirm their existing authenticator.
- Ensure skip/required behavior is unchanged, but the messaging aligns with automatic setup.
  </action>
  <verify>Run `bun run typecheck` in `packages/app`.</verify>
  <done>Messaging reflects auto-setup status and only references manual steps when the broker fallback is active.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` in `packages/app`.
</verification>

<success_criteria>

- Setup UI defaults to auto-setup status and hides the command step.
- Manual command appears only when the broker cannot create the file.
- Already-configured users see a clear status without overwriting their file.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-during-2fa-setup-make-the-server-automatically-set-the-google-authenticator-file-in-the-appropriate-user-s-home-folder-instead-of-asking-the-user-to-run-the-command-on-their-machine/23-02-SUMMARY.md`
</output>

---
phase: 24-remote-terminal-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/server/routes/pty.ts
  - packages/opencode/src/pty/index.ts
  - packages/opencode/src/pty/broker-pty.ts
  - packages/opencode/src/auth/broker-client.ts
  - packages/app/src/context/terminal.tsx
  - packages/app/src/components/terminal.tsx
autonomous: true

must_haves:
  truths:
    - "PTY create failures return a stable error code and request id."
    - "Broker session/PTY lifecycle logs include sessionId + ptyId correlation."
    - "UI logs include the PTY request id when create/connect fails."
  artifacts:
    - path: "packages/opencode/src/server/routes/pty.ts"
      provides: "PTY create/update/connect error shaping with request ids"
    - path: "packages/opencode/src/pty/index.ts"
      provides: "PTY create/lookup logging with sessionId + ptyId"
    - path: "packages/app/src/context/terminal.tsx"
      provides: "Terminal create error handling with request id surfaced"
  key_links:
    - from: "packages/opencode/src/server/routes/pty.ts"
      to: "packages/opencode/src/pty/index.ts"
      via: "Pty.create/Pty.connect error handling"
      pattern: "Pty\\.create|Pty\\.connect"
    - from: "packages/app/src/context/terminal.tsx"
      to: "packages/opencode/src/server/routes/pty.ts"
      via: "sdk.client.pty.create"
      pattern: "sdk\\.client\\.pty\\.create"
---

<objective>
Add diagnostics that correlate UI, server, and broker PTY session creation so "session not found" and /pty 500s produce actionable errors with request ids.

Purpose: Enable reliable triage of PTY session creation failures across UI/server/broker.
Output: Structured logs, error codes, and client-visible request ids for PTY lifecycle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/opencode/src/server/routes/pty.ts
@packages/opencode/src/pty/index.ts
@packages/opencode/src/pty/broker-pty.ts
@packages/opencode/src/auth/broker-client.ts
@packages/app/src/context/terminal.tsx
@packages/app/src/components/terminal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add request-scoped logging for PTY lifecycle</name>
  <files>packages/opencode/src/server/routes/pty.ts, packages/opencode/src/pty/index.ts, packages/opencode/src/pty/broker-pty.ts, packages/opencode/src/auth/broker-client.ts</files>
  <action>
    Introduce a request id per PTY create/connect request in the server route, attach it to logs, and propagate it into PTY/broker logging. Ensure logs include sessionId, ptyId, and broker method name when available. Map broker "session not found" failures to a stable error code and include request id in the response body.
  </action>
  <verify>Manual inspection: open terminal in app, observe logs with request id + sessionId + ptyId.</verify>
  <done>PTY create/connect logs are correlated across server + PTY modules and responses include request ids on failures.</done>
</task>

<task type="auto">
  <name>Task 2: Surface request id in terminal UI errors</name>
  <files>packages/app/src/context/terminal.tsx, packages/app/src/components/terminal.tsx</files>
  <action>
    When PTY create/connect fails, capture the server error payload and log/notify with the request id and error code. Ensure the UI does not swallow the error silently and keeps the failed PTY entry clearly marked for retry.
  </action>
  <verify>Manual test: force a PTY create failure and confirm request id is shown in console/toast.</verify>
  <done>UI shows a request id and error code for PTY failures, enabling cross-reference with server logs.</done>
</task>

</tasks>

<verification>
- Confirm PTY failures return JSON error with `code` + `requestId`.
- Confirm server logs include the same request id for create/connect and broker calls.
</verification>

<success_criteria>

- PTY failure responses are actionable (code + requestId).
- Logs allow tracing UI -> server -> broker with shared request id.
  </success_criteria>

<output>
After completion, create `.planning/phases/24-remote-terminal-reliability/24-01-SUMMARY.md`
</output>

---
phase: 24-remote-terminal-reliability
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/opencode/src/pty/index.ts
  - packages/opencode/src/pty/broker-pty.ts
  - packages/opencode/src/server/routes/pty.ts
  - packages/opencode/src/server/routes/auth.ts
  - packages/app/src/context/terminal.tsx
  - packages/app/src/components/terminal.tsx
  - packages/opencode/test/server/routes/pty-auth.test.ts
  - packages/opencode/test/server/routes/pty-broker.test.ts
autonomous: false

must_haves:
  truths:
    - "Auth-enabled PTY creation does not return /pty 500s during normal use."
    - "Broker session registration and PTY spawn are consistently aligned to the same sessionId."
    - "Terminal UI reconnects or retries without stale PTY ids."
  artifacts:
    - path: "packages/opencode/src/pty/index.ts"
      provides: "Broker-backed PTY creation path without TODO/throw"
      contains: "createViaBroker"
    - path: "packages/opencode/src/pty/broker-pty.ts"
      provides: "Broker PTY lifecycle tracking and WebSocket I/O"
    - path: "packages/opencode/test/server/routes/pty-broker.test.ts"
      provides: "PTY error mapping and broker fallback tests"
  key_links:
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/opencode/src/auth/broker-client.ts"
      via: "registerSession on login"
      pattern: "registerSession"
    - from: "packages/opencode/src/server/routes/pty.ts"
      to: "packages/opencode/src/pty/broker-pty.ts"
      via: "broker-backed PTY create/connect"
      pattern: "BrokerPty"
    - from: "packages/app/src/context/terminal.tsx"
      to: "packages/opencode/src/server/routes/pty.ts"
      via: "pty.create/pty.remove lifecycle"
      pattern: "sdk\\.client\\.pty\\.(create|remove)"
---

<objective>
Fix PTY broker session lifecycle and UI/server ID mismatches so authenticated terminal creation and connection succeed reliably.

Purpose: Eliminate "session not found" and 500 errors in remote terminal usage.
Output: Broker-backed PTY wiring, session lifecycle fixes, tests, and UAT validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/opencode/src/server/routes/pty.ts
@packages/opencode/src/pty/index.ts
@packages/opencode/src/pty/broker-pty.ts
@packages/opencode/src/server/routes/auth.ts
@packages/app/src/context/terminal.tsx
@packages/app/src/components/terminal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire broker-backed PTY creation/connection</name>
  <files>packages/opencode/src/pty/index.ts, packages/opencode/src/pty/broker-pty.ts, packages/opencode/src/server/routes/pty.ts</files>
  <action>
    Replace the broker create TODO path with BrokerPty-backed creation and connection. Ensure Pty.create/pty.connect/pty.update/pty.remove route correctly to broker sessions when auth is enabled and a sessionId is present. Preserve local bun-pty path when auth is disabled. Ensure broker PTY info maps to Pty.Info consistently, and add guardrails when a broker session is missing (return a typed error instead of throwing).
  </action>
  <verify>Manual test: with auth enabled, create terminal and confirm /pty returns 200 and /pty/:id/connect opens WS.</verify>
  <done>Broker-backed PTY creation succeeds with valid session registration and no TODO/throw path remains.</done>
</task>

<task type="auto">
  <name>Task 2: Align session registration lifecycle and UI PTY id handling</name>
  <files>packages/opencode/src/server/routes/auth.ts, packages/opencode/src/server/routes/pty.ts, packages/app/src/context/terminal.tsx, packages/app/src/components/terminal.tsx</files>
  <action>
    Ensure broker session registration is completed (or retried) before PTY spawn when auth is enabled. If the broker is unavailable or session is missing, return a clear error code and prevent PTY creation. In the UI, avoid reusing stale PTY ids on reconnect/clone and ensure replacement uses the new id returned from the server.
  </action>
  <verify>Manual test: log in, open terminal, refresh page, verify terminal reconnects without 500s or stale ids.</verify>
  <done>PTY creation uses a registered sessionId and the UI uses the latest server-issued PTY id.</done>
</task>

<task type="auto">
  <name>Task 3: Add server tests and manual UAT checklist</name>
  <files>packages/opencode/test/server/routes/pty-auth.test.ts, packages/opencode/test/server/routes/pty-broker.test.ts</files>
  <action>
    Add tests for PTY error mapping and broker session missing behavior (mock broker client). Ensure auth-on PTY create returns the expected error code when broker session is missing and success when session is registered. Include a manual UAT checklist in the plan summary for terminal creation, reconnect, and close flows.
  </action>
  <verify>Run: `bun run --cwd packages/opencode test test/server/routes/pty-auth.test.ts test/server/routes/pty-broker.test.ts`</verify>
  <done>Tests cover broker session error mapping and UAT checklist exists for terminal reliability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Broker-backed PTY wiring, session lifecycle fixes, UI ID handling, and server tests.</what-built>
  <how-to-verify>
    1) Open the app at http://localhost:3000 and log in.
    2) Open the terminal panel and create a new terminal tab.
    3) Verify the terminal connects and can run a command (e.g., `pwd`).
    4) Refresh the page and ensure the terminal reconnects without /pty 500s.
    5) Close the terminal tab and confirm server logs show PTY cleanup.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Tests: `bun run --cwd packages/opencode test test/server/routes/pty-auth.test.ts test/server/routes/pty-broker.test.ts`
- UAT steps above.
</verification>

<success_criteria>

- Auth-enabled PTY creation/connect works without "session not found".
- UI does not reuse stale PTY ids after reconnect/clone.
- Tests validate broker error mapping and session registration handling.
  </success_criteria>

<output>
After completion, create `.planning/phases/24-remote-terminal-reliability/24-02-SUMMARY.md`
</output>

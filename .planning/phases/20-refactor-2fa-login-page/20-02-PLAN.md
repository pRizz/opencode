---
phase: 20-refactor-2fa-login-page
plan: 02
type: execute
wave: 2
depends_on: ["20-refactor-2fa-login-page-01"]
files_modified:
  - packages/opencode/src/server/routes/auth.ts
autonomous: true
must_haves:
  truths:
    - "GET /auth/login/totp serves the SolidJS-based TOTP verification page (legacy /auth/login/2fa alias retained)"
    - "TOTP bootstrap data (token, username, timeout) is injected into the page per request"
    - "The string-based generate2FAPageHtml template is removed from auth routes"
  artifacts:
    - path: "packages/opencode/src/server/routes/auth.ts"
      provides: "TOTP HTML loader and bootstrap injector"
      contains: "2fa.html"
    - path: "packages/app/dist/2fa.html"
      provides: "Built TOTP page served by auth route"
      contains: "2fa.html"
  key_links:
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/opencode/src/server/ui-dir.ts"
      via: "getUiDir()"
      pattern: "getUiDir"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/app/dist/2fa.html"
      via: "filesystem read and HTML injection"
      pattern: "2fa.html"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "window.__OPENCODE_TOTP__"
      via: "bootstrap script injection"
      pattern: "__OPENCODE_TOTP__"
---

<objective>
Serve the built SolidJS TOTP verification page from the auth route and remove the existing string template.

Purpose: Ensure `/auth/login/totp` uses the built UI output and stays aligned with the login page refactor pattern (legacy `/auth/login/2fa` alias retained).
Output: Auth route reads `2fa.html` from the UI build and injects TOTP bootstrap data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-auth-login-page/19-02-SUMMARY.md
@packages/opencode/src/server/routes/auth.ts
@packages/opencode/src/server/ui-dir.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOTP HTML loader and bootstrap injector</name>
  <files>packages/opencode/src/server/routes/auth.ts</files>
  <action>
Replace `generate2FAPageHtml` with a loader that reads the built `2fa.html`:
- Use `getUiDir()` to resolve the UI build directory and a cached template for `2fa.html`.
- Read `2fa.html` from the UI dir; cache the template string and path similarly to login.
- Inject a bootstrap script like `window.__OPENCODE_TOTP__ = { token, username, timeoutSeconds }` before `</head>` or `</body>`.
- If the UI dir or `2fa.html` is missing, return a clear error response (do not fall back to the old template).
Keep the existing error handling and escape behavior for user-supplied values; avoid new dependencies.
  </action>
  <verify>In `packages/opencode`, run `bun run typecheck` to ensure the new helpers compile.</verify>
  <done>Auth route can load `2fa.html` and inject per-request bootstrap data.</done>
</task>

<task type="auto">
  <name>Task 2: Serve built TOTP HTML from GET /auth/login/totp (legacy /auth/login/2fa alias)</name>
  <files>packages/opencode/src/server/routes/auth.ts</files>
  <action>
Update the `/auth/login/totp` GET route (and keep legacy `/auth/login/2fa` alias) to use the new loader and injector:
- Keep the query param handling for `token`, `username`, and `timeout`.
- Load the cached `2fa.html`, inject `{ token, username, timeoutSeconds }`, and return via `c.html()`.
- Remove the old `generate2FAPageHtml` template and any unused helpers.
  </action>
  <verify>Run `bun run build` in `packages/app` and confirm `/auth/login/totp` (or legacy `/auth/login/2fa`) serves `2fa.html` in local mode.</verify>
  <done>Auth route serves the Solid TOTP page and no inline TOTP template remains.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` in `packages/opencode` succeeds.
- `bun run build` in `packages/app` produces `dist/2fa.html` that is served for `/auth/login/totp` (legacy `/auth/login/2fa` alias retained).
</verification>

<success_criteria>

- `/auth/login/totp` responds with the SolidJS TOTP page using injected bootstrap data (legacy `/auth/login/2fa` alias retained).
- The string-based TOTP HTML template is removed.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-2fa-login-page/20-02-SUMMARY.md`
</output>

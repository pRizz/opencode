---
phase: 20-refactor-2fa-login-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/totp.html
  - packages/app/src/totp/index.tsx
  - packages/app/src/totp/verify.tsx
  - packages/app/vite.config.ts
autonomous: true
must_haves:
  truths:
    - "TOTP verification page renders with the same layout and UX as the current inline HTML page"
    - "TOTP verification uses bootstrap data for token, username, and timeout countdown"
    - "TOTP verification auto-submits on 6-digit codes and supports backup code entry"
  artifacts:
    - path: "packages/app/totp.html"
      provides: "TOTP HTML entry with root element and module script"
      contains: "/src/totp/index.tsx"
    - path: "packages/app/src/totp/index.tsx"
      provides: "Solid entry that renders the TOTP app"
      exports: ["TotpApp", "TwoFactorApp (legacy alias)"]
    - path: "packages/app/src/totp/verify.tsx"
      provides: "TOTP verification UI and form logic"
      contains: "rememberDevice"
    - path: "packages/app/vite.config.ts"
      provides: "Multi-page build input for TOTP entry"
      contains: "rollupOptions.input"
  key_links:
    - from: "packages/app/totp.html"
      to: "packages/app/src/totp/index.tsx"
      via: "module script tag"
      pattern: 'src="/src/totp/index.tsx"'
    - from: "packages/app/src/totp/verify.tsx"
      to: "/auth/login/totp"
      via: "fetch POST with X-Requested-With header"
      pattern: "X-Requested-With"
    - from: "packages/app/src/totp/index.tsx"
      to: "window.__OPENCODE_TOTP__"
      via: "bootstrap data read"
      pattern: "__OPENCODE_TOTP__"
---

<objective>
Create a SolidJS TOTP verification entry in `packages/app` that mirrors the current inline HTML page and can be built as a standalone `totp.html` (with legacy `2fa.html` compatibility).

Purpose: Move the TOTP verification UI out of the server string template into the app build for easier maintenance and consistency.
Output: A Solid TOTP entry, component, and Vite build input for `totp.html`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-auth-login-page/19-02-SUMMARY.md
@packages/app/src/login/login.tsx
@packages/opencode/src/server/routes/auth.ts
@packages/app/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Solid TOTP verification UI with parity behavior</name>
  <files>packages/app/src/totp/index.tsx, packages/app/src/totp/verify.tsx</files>
  <action>
Create a SolidJS TOTP entry and component that match the current inline TOTP verification template UX:
- Add `TotpApp` that renders into `#root` and imports `@/index.css` for Tailwind styles (keep `TwoFactorApp` as a legacy alias export).
- Read bootstrap data from `window.__OPENCODE_TOTP__` with `{ token, username, timeoutSeconds }`.
- Implement the same layout, logo, and card structure as the inline HTML page, keeping typography and spacing aligned.
- Implement countdown timer behavior with warning/critical states and redirect to `/auth/login` on expiry.
- Implement code input: digits only, auto-submit when 6 digits are entered, allow 6-8 digit backup codes.
- Include remember-device checkbox and wire `rememberDevice` into the POST body.
- Submit via `fetch("/auth/login/totp")` with `Content-Type: application/json` and `X-Requested-With` header (legacy `/auth/login/2fa` alias retained).
- On success, redirect to `/`; on `token_expired`, redirect to `/auth/login`; otherwise surface error state and re-enable the button.
Avoid new dependencies and avoid UI redesigns; focus on parity with the existing TOTP page.
  </action>
  <verify>In `packages/app`, run `bun run build` and ensure it compiles without errors.</verify>
  <done>Solid TOTP entry renders with matching layout, countdown behavior, and submission flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add TOTP HTML entry and Vite multi-page build input</name>
  <files>packages/app/totp.html, packages/app/vite.config.ts</files>
  <action>
Add `totp.html` at the package root:
- Mirror `login.html` head metadata and favicon links.
- Set title to "Verification - opencode".
- Include a `#root` element and module script pointing to `/src/totp/index.tsx`.
Update `vite.config.ts` to add `totp.html` to `build.rollupOptions.input` alongside `index.html` and `login.html` (keep legacy `2fa.html` compatibility input).
Keep existing plugin configuration intact.
  </action>
  <verify>In `packages/app`, run `bun run build` and confirm `dist/totp.html` exists.</verify>
  <done>Vite build emits `dist/totp.html` with the TOTP entry script.</done>
</task>

</tasks>

<verification>
- `bun run build` in `packages/app` succeeds and outputs `dist/totp.html`.
</verification>

<success_criteria>

- Solid TOTP entry matches current TOTP UI and behavior.
- Multi-page build produces a standalone `totp.html` entry.
  </success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-2fa-login-page/20-01-SUMMARY.md`
</output>

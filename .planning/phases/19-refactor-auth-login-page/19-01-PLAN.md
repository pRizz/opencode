---
phase: 19-refactor-auth-login-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/login.html
  - packages/app/src/login/index.tsx
  - packages/app/src/login/login.tsx
  - packages/app/vite.config.ts
autonomous: true
must_haves:
  truths:
    - "Login page renders with the same layout and styling as the current page"
    - "HTTP warning dismissal and HTTPS block behavior match current login UX"
    - "Login submits credentials with remember-me default and handles TOTP redirects"
    - "Login layout remains mobile responsive"
  artifacts:
    - path: "packages/app/login.html"
      provides: "Login HTML entry with root element and module script"
      contains: "/src/login/index.tsx"
    - path: "packages/app/src/login/index.tsx"
      provides: "Solid login entry rendering LoginApp"
      exports: ["LoginApp"]
    - path: "packages/app/src/login/login.tsx"
      provides: "Login UI component with form logic and warning handling"
      contains: "http-warning-dismissed"
    - path: "packages/app/vite.config.ts"
      provides: "Multi-page build input for login entry"
      contains: "rollupOptions.input"
  key_links:
    - from: "packages/app/login.html"
      to: "packages/app/src/login/index.tsx"
      via: "module script tag"
      pattern: 'src="/src/login/index.tsx"'
    - from: "packages/app/src/login/login.tsx"
      to: "/auth/login"
      via: "fetch POST with X-Requested-With header"
      pattern: "X-Requested-With"
    - from: "packages/app/src/login/login.tsx"
      to: "sessionStorage"
      via: "http warning dismissal"
      pattern: "http-warning-dismissed"
---

<objective>
Create a SolidJS login entry in `packages/app` that matches the existing login UI and behavior while fitting the app stack.

Purpose: Replace the string-based login markup with a SolidJS UI that is easier to maintain and consistent with the rest of the app.
Output: A new Solid login entry and Vite multi-page build wiring for `login.html`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-auth-login-page/19-CONTEXT.md
@.planning/phases/19-refactor-auth-login-page/19-RESEARCH.md
@packages/app/src/entry.tsx
@packages/app/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Solid login UI with parity behavior</name>
  <files>packages/app/src/login/index.tsx, packages/app/src/login/login.tsx</files>
  <action>
Create a new Solid login entry and component that mirror the current login page:
- Add `LoginApp` that renders into `#root` and imports `@/index.css` for Tailwind styles.
- Use `createStore` for form state (username, password, rememberMe, submitting, error, showPassword, invalid flags).
- Read server-provided bootstrap data from `window.__OPENCODE_LOGIN__` (shouldWarn, shouldBlock) and drive warning/block UI from it.
- Implement HTTP warning dismissal with sessionStorage key `http-warning-dismissed` (same behavior as current page).
- Preserve form validation, password toggle, remember-me default checked, and submit disabled state when blocked.
- Submit via `fetch("/auth/login")` with `Content-Type: application/json` and `X-Requested-With` header; handle `2fa_required` and `2fa_setup_required` responses exactly as the current template does (redirects and button state).
- Keep the logo and typography matching the current login page (reuse existing SVG markup or the existing UI logo if it is visually identical).
Avoid new dependencies and avoid UI redesigns; focus on parity and SolidJS patterns.
  </action>
  <verify>In `packages/app`, run `bun run build` and ensure it compiles without errors.</verify>
  <done>Solid login entry renders with matching layout, warning behavior, and form submission flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add login HTML entry and Vite multi-page build input</name>
  <files>packages/app/login.html, packages/app/vite.config.ts</files>
  <action>
Add `login.html` at the package root:
- Mirror `index.html` head metadata and favicon links.
- Set title to "Login - opencode".
- Include a `#root` element and module script pointing to `/src/login/index.tsx`.
Update `vite.config.ts` to add `build.rollupOptions.input` so both `index.html` and `login.html` are emitted in the build output.
Keep existing plugin configuration intact.
  </action>
  <verify>In `packages/app`, run `bun run build` and confirm `dist/login.html` exists.</verify>
  <done>Vite build emits both `index.html` and `login.html` with the login entry script.</done>
</task>

</tasks>

<verification>
- `bun run build` in `packages/app` succeeds and outputs `dist/login.html`.
</verification>

<success_criteria>

- Solid login entry matches current login UI and behavior.
- Multi-page build produces a standalone `login.html` entry.
  </success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-auth-login-page/19-01-SUMMARY.md`
</output>

---
phase: 22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app
plan: 02
type: execute
wave: 2
depends_on: ["22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app-01"]
files_modified:
  - packages/opencode/src/server/routes/auth.ts
autonomous: true
must_haves:
  truths:
    - "GET /auth/totp/setup serves the SolidJS-based TOTP setup page (legacy /auth/2fa/setup alias retained)"
    - "TOTP setup bootstrap data is injected per request"
    - "The string-based TOTP setup HTML template is removed from auth routes"
  artifacts:
    - path: "packages/opencode/src/server/routes/auth.ts"
      provides: "TOTP setup HTML loader and bootstrap injector"
      contains: "2fa-setup.html"
    - path: "packages/app/dist/2fa-setup.html"
      provides: "Built TOTP setup page served by auth route"
      contains: "2fa-setup.html"
  key_links:
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/opencode/src/server/ui-dir.ts"
      via: "getUiDir()"
      pattern: "getUiDir"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/app/dist/2fa-setup.html"
      via: "filesystem read and HTML injection"
      pattern: "2fa-setup.html"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "window.__OPENCODE_TOTP_SETUP__"
      via: "bootstrap script injection"
      pattern: "__OPENCODE_TOTP_SETUP__"
---

<objective>
Serve the built SolidJS TOTP setup page from the auth route and remove the existing string template.

Purpose: Ensure `/auth/totp/setup` uses the built UI output with bootstrap injection, matching the login/TOTP refactor pattern (legacy `/auth/2fa/setup` alias retained).
Output: Auth route reads `2fa-setup.html` from the UI build and injects TOTP setup bootstrap data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-refactor-2fa-login-page/20-02-SUMMARY.md
@packages/opencode/src/server/routes/auth.ts
@packages/opencode/src/server/ui-dir.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOTP setup HTML loader and bootstrap injector</name>
  <files>packages/opencode/src/server/routes/auth.ts</files>
  <action>
Replace `generate2FASetupPageHtml` with a loader that reads the built `2fa-setup.html`:
- Use `getUiDir()` to resolve the UI build directory and a cached template for `2fa-setup.html`.
- Read `2fa-setup.html` from the UI dir; cache the template string and path similarly to login and TOTP.
- Inject a bootstrap script like `window.__OPENCODE_TOTP_SETUP__ = { username, secret, qrCodeSvg, setupCommand, alreadyConfigured, required }` before `</head>` or `</body>`.
- If the UI dir or `2fa-setup.html` is missing, return a clear error response (do not fall back to the old template).
Keep existing session checks, TOTP detection, and setup data generation; avoid new dependencies.
  </action>
  <verify>In `packages/opencode`, run `bun run typecheck` to ensure the new helpers compile.</verify>
  <done>Auth route can load `2fa-setup.html` and inject per-request bootstrap data.</done>
</task>

<task type="auto">
  <name>Task 2: Serve built TOTP setup HTML from GET /auth/totp/setup (legacy /auth/2fa/setup alias)</name>
  <files>packages/opencode/src/server/routes/auth.ts</files>
  <action>
Update the `/auth/totp/setup` GET route (and keep legacy `/auth/2fa/setup` alias) to use the new loader and injector:
- Keep the session checks, `required` query param handling, and `generateTotpSetup` call.
- Load the cached `2fa-setup.html`, inject the bootstrap object, and return via `c.html()`.
- Remove the old `generate2FASetupPageHtml` template and any unused helpers (including inline CSS or escape helpers only used by that template).
  </action>
  <verify>Run `bun run build` in `packages/app` and confirm `/auth/totp/setup` (or legacy `/auth/2fa/setup`) serves `2fa-setup.html` in local mode.</verify>
  <done>Auth route serves the Solid TOTP setup page and no inline setup template remains.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` in `packages/opencode` succeeds.
- `bun run build` in `packages/app` produces `dist/2fa-setup.html` that is served for `/auth/totp/setup` (legacy `/auth/2fa/setup` alias retained).
</verification>

<success_criteria>

- `/auth/totp/setup` responds with the SolidJS TOTP setup page using injected bootstrap data (legacy `/auth/2fa/setup` alias retained).
- The string-based TOTP setup HTML template is removed.
  </success_criteria>

<output>
After completion, create `.planning/phases/22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app/22-02-SUMMARY.md`
</output>

---
phase: 22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/totp-setup.html
  - packages/app/src/totp-setup/index.tsx
  - packages/app/src/totp-setup/setup.tsx
  - packages/app/vite.config.ts
autonomous: true
must_haves:
  truths:
    - "TOTP setup page renders with the same layout and UX as the current inline HTML page"
    - "TOTP setup page uses bootstrap data for username, QR code, secret, and setup command"
    - "Verify and skip actions behave identically to the current setup page flow"
  artifacts:
    - path: "packages/app/totp-setup.html"
      provides: "TOTP setup HTML entry with root element and module script"
      contains: "/src/totp-setup/index.tsx"
    - path: "packages/app/src/totp-setup/index.tsx"
      provides: "Solid entry that renders the TOTP setup app"
      exports: ["TotpSetupApp", "TwoFactorSetupApp (legacy alias)"]
    - path: "packages/app/src/totp-setup/setup.tsx"
      provides: "TOTP setup UI and form logic"
      contains: "window.__OPENCODE_TOTP_SETUP__"
    - path: "packages/app/vite.config.ts"
      provides: "Multi-page build input for TOTP setup entry"
      contains: "rollupOptions.input"
  key_links:
    - from: "packages/app/totp-setup.html"
      to: "packages/app/src/totp-setup/index.tsx"
      via: "module script tag"
      pattern: 'src="/src/totp-setup/index.tsx"'
    - from: "packages/app/src/totp-setup/setup.tsx"
      to: "window.__OPENCODE_TOTP_SETUP__"
      via: "bootstrap data read"
      pattern: "__OPENCODE_TOTP_SETUP__"
    - from: "packages/app/src/totp-setup/setup.tsx"
      to: "/auth/totp/verify"
      via: "fetch POST with X-Requested-With and X-CSRF-Token"
      pattern: "X-CSRF-Token"
    - from: "packages/app/src/totp-setup/setup.tsx"
      to: "/auth/totp/skip"
      via: "fetch POST with X-Requested-With and X-CSRF-Token"
      pattern: "X-Requested-With"
---

<objective>
Create a SolidJS TOTP setup entry in `packages/app` that mirrors the existing inline setup page and can be built as a standalone `totp-setup.html` (with legacy `2fa-setup.html` compatibility).

Purpose: Move the TOTP setup wizard out of the server string template into the app build while preserving the exact UX.
Output: A Solid TOTP setup entry, component, and Vite build input for `totp-setup.html`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-auth-login-page/19-02-SUMMARY.md
@.planning/phases/20-refactor-2fa-login-page/20-01-SUMMARY.md
@packages/opencode/src/server/routes/auth.ts
@packages/app/src/totp/verify.tsx
@packages/app/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Solid TOTP setup UI with parity behavior</name>
  <files>packages/app/src/totp-setup/index.tsx, packages/app/src/totp-setup/setup.tsx</files>
  <action>
Create a SolidJS TOTP setup entry and component that match the current inline TOTP setup template UX:
- Add `TwoFactorSetupApp` that renders into `#root` and imports `@/index.css` for base styles.
- Read bootstrap data from `window.__OPENCODE_TOTP_SETUP__` containing `{ username, secret, qrCodeSvg, setupCommand, alreadyConfigured, required }`.
- Mirror the existing inline layout (card, typography, spacing) with inline CSS, matching the current look and mobile responsiveness.
- Render the QR SVG markup from bootstrap (use `innerHTML` or Solid `<div innerHTML={...}>`) and show the secret with the same monospace styling.
- Include the existing install instructions and PAM service setup details (the two `<details>` blocks and copytext should match the current HTML).
- Implement copy button behavior for the setup command, including clipboard fallback and temporary "Copied!" state.
- Build the verify form: digits-only input, auto-submit on 6 digits, POST to `/auth/totp/verify` (legacy `/auth/2fa/verify` alias retained) with `Content-Type: application/json`, `X-Requested-With`, and `X-CSRF-Token` (read from the CSRF cookie).
- On verify success, show the success message and redirect to `/` after a short countdown (same as current template); on error, show the same message and re-enable inputs.
- Render the required banner and "already configured" warning with the same conditions; hide the skip section when `required` is true.
- Implement "Skip for now" button to POST `/auth/totp/skip` (legacy `/auth/2fa/skip` alias retained) with CSRF headers and redirect to `/` on success; show error and re-enable on failure.
- Keep the back link text consistent: "Back to login" when required, otherwise "Back to opencode".
Avoid new dependencies and avoid UI redesigns; use existing Solid patterns (createStore, inline styles) for parity.
  </action>
  <verify>In `packages/app`, run `bun run build` and ensure it compiles without errors.</verify>
  <done>Solid TOTP setup entry renders with matching layout, QR/secret display, verify/skip flows, and copy behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add TOTP setup HTML entry and Vite multi-page build input</name>
  <files>packages/app/totp-setup.html, packages/app/vite.config.ts</files>
  <action>
Add `totp-setup.html` at the package root:
- Mirror the head metadata and favicon links from `login.html` and `totp.html`.
- Set title to "Set Up TOTP Authentication - opencode".
- Include a `#root` element and module script pointing to `/src/totp-setup/index.tsx`.
Update `vite.config.ts` to include `totp-setup.html` in `build.rollupOptions.input` alongside `index.html`, `login.html`, and `totp.html` (keep legacy `2fa-setup.html` compatibility input).
Keep existing plugin configuration intact.
  </action>
  <verify>In `packages/app`, run `bun run build` and confirm `dist/totp-setup.html` exists.</verify>
  <done>Vite build emits `dist/totp-setup.html` with the setup entry script.</done>
</task>

</tasks>

<verification>
- `bun run build` in `packages/app` succeeds and outputs `dist/totp-setup.html`.
</verification>

<success_criteria>

- Solid TOTP setup entry matches current setup UI and behavior.
- Multi-page build produces a standalone `totp-setup.html` entry.
  </success_criteria>

<output>
After completion, create `.planning/phases/22-refactor-the-auth-2fa-setup-page-from-the-auth-ts-into-the-solidjs-app-at-packages-app/22-01-SUMMARY.md`
</output>
